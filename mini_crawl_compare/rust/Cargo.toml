[package]
name = "mini-crawl-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12", default-features = true }
tokio = { version = "1", features = ["fs", "macros", "rt-multi-thread", "sync"] }
